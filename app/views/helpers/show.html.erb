<style>
  .helper-info__photo-wrapper {
      margin: var(--spacing) 0;
      display: flex;
      justify-content: center;
  }

  .helper-info__photo {
    border: 1px solid var(--gray-300);
    border-radius: var(--border-radius);
    max-height: 256px;
  }

  /* desktop styles */
  @media(min-width: 768px) {
  }
</style>

<div style="display: block; margin-bottom: 2rem;">
  <%= link_to "< Zurück zu allen Mitarbeitern", helpers_path %>
</div>

<h1><%= "#{@helper.surname}, #{@helper.forename}" %></h1>

<div class="helper-info">
  <% if @helper.photo.attached? %>
    <div class="helper-info__photo-wrapper">
      <%= image_tag @helper.photo, class: 'helper-info__photo' %>
    </div>
  <% end %>

  <%= render @helper %>
  <%= link_to "Mitarbeiter bearbeiten", edit_helper_path(@helper) %>
  <br>
  <br>

  <div>
    <h2>Camp anmeldungen</h2>
    <div>
      <% @helper.registrations.each do |registration| %>
        <%= render 'registrations/registration', registration: registration %>
        <%= link_to 'Anmeldung bearbeiten', edit_admin_helper_registration_path(registration) %>
        <br>
      <% end %>
    </div>
  </div>
</div>

<%= render "formelements/delete_action", url: @helper %>
